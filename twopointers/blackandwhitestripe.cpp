#include <bits/stdc++.h>
#define ll long long int
#define fastread() (ios_base::sync_with_stdio(false), cin.tie(NULL));
using namespace std;

int main() 
{
    ll t;
    cin >> t;
    while(t!=0)
    {
        ll n,k;
        cin >> n>>k;
        string a;
        cin >>a;
        // this can be done in a fixed length sliding window problems
        for(int i =0;i<k;i++)
        {
            
        }
        t--;
    }   
    return 0;
}